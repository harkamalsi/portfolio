{
  "scripts": {
    "start": "concurrently \"cd backend && npm run start\" \"cd frontend && npm run build && npm run serve\"",
    "build": "concurrently \"cd backend && npm run build\" \"cd frontend && npm run build\"",
    "build-gatsby": "cd frontend && npm run build",
    "start-strapi": "cd backend && npm run start",
    "develop-strapi": "cd backend && npm run develop"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
